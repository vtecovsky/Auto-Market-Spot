# __all__ = ["router_auth", "router_auth_jwt"]
#
# from fastapi import APIRouter
#
# router_auth = APIRouter(prefix="/auth", tags=["Auth"])
# router_auth_jwt = APIRouter(prefix="/auth/jwt", tags=["Auth"])
#
# # Register all schemas and routes
# import src.auth.routers  # noqa: E402
